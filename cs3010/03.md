---
title: "Finding Zeroes of a Function"
---

We know how to manually solve quadratic equations to find zero.
- But, how do you do it for all continuous functions?

# Bisection Method

TODO Cleanup

**Step 0**

Suppose you want to find two points where this test holds:

$$
f(x_1) f(x_2) < 0
$$

> This tests for two points that enclose a zero.

If we find these two points, we can squeeze-in on the midpoint ($x_3$) to find the zero.

**Step 1**: Get midpoint

$$
x_i = \frac{x_{i-1} + x_{i-2}}{2}
$$

**Step 2**: Have we found the zero?

If:

$$
	f(x_i) = 0 \\
	\text{ or } \\
	|x_{i-2} - x_{i-1}| < \epsilon
$$

> We can $x_i$ is the zero if it literally is zero, or it is within our acceptable range of error.

**Step 3**: Shrink window

If $f(x_i) * f(x_{i - 1}) > 0$ then

$$
x_{i-1} = x_{i-2}
$$

**Step 4**: Loop

Now, go to **Step 1**

---

Example

$$
f(x) = 3x^2 -4x + 1
$$

**Step 0**:

$x_0 = 0.5$, $x_1 = 1.8$

$$
f(x_0) = -0.25 \times 3.53 < 0
$$

**Iteration 1:**

$$
x_2 = \frac{x_1 + x_0}{2} = 1.15
$$

$$
f(1.15) = 0.3675
$$

Not zero or in acceptabe range, so we'll shrink.

$$
x_i = 0.5
$$

**Iteration 2**:

$$
x_3 = \frac{x_1 + x_2}{2} = \frac{0.5 + 1.15}{2} = 0.825
$$

$$
f(0.825) = -0.258125
$$

Shrink again.

**Iteration 3**:

$$
x_4 = \frac{x_3 + x_2}{2} = \frac{0.825 + 1.15}{2} = 0.9875
$$

$$
f(x_4) = f(0.9875) = - 0.02453125
$$

Shrink again

$
x_3 = 1.15
$

**Iteration 4:**

$$
x_5 = \frac{x_3 + x_4}{2} = \frac{0.9875 + 1.15}{2} = 1.06875
$$

$$
f(x_5) = f(1.06875) = 0.1516796875
$$

We just lost the zero!

---

> **Problems**
> 
> This problem is very slow, and kinda sucks.
> 
> Next class, Newton's algorithm. We go from needing 100 iterations to like 5.

## Pseudocode

TODO

Notice in the code we do

$$
x_i = \frac{x_{i-2} - x_{i-1}}{2} + x_{i-1}
$$

This is more verbose than our exaplanation, because that numerator is the error, letting us kill two birds with one stone.

# Newton's Method

$$
x = x_0 - \frac{f(x_0)}{f'(x_0)}
$$

$$
x_i = x_{i-1} - \frac{f(x_{i-1})}{f'(x_{i-1})}
$$

> **Example**: How the iteration formula was derived
> 
> We use the derivative to find the tangent line in order to drastically speed up.
> 
> $$
> \text{Equation of Line: } y - y_0 = m (x - x_0)
> $$
> 
> Plugging in the tangent of $x_0$:
> 
> $$
> y - f(x_0) = f'(x_0) (x - x_0)
> $$
> 
> This tangent line cuts really close to zero.
> 
> So, we just want to find the point where the line becomes zero:
> 
> $$
> 0 - f(x_0) = f'(x_0) (x - x_0)
> $$
> 
> Solving for $x$, we get:
> 
> $$
> x = x_0 - \frac{f(x_0)}{f'(x_0)}
> $$
> 
> > We get closer and closer, throwing lines torwards the zero.
> 
> This is how we derived the iteration formula:
> 
> $$
> x_i = x_{i-1} - \frac{f(x_{i-1})}{f'(x_{i-1})}
> $$

> **Example**: Newton's Method
> 
> Q:
> 
> $f(x) = 3x^2 - 4x + 1$
> 
> $x_0 = 1.8$
> 
> A:
> 
> $$
> f'x() = 6x - 4
> $$
> 
> $$
> f(x_0) = 3.52\\
> f'(x_0) = 6.8
> $$
> 
> **Iteration 1:**
> 
> $$
> x_1 = 1.8 - \frac{3.52}{6.8} = 1.282352947
> $$
> 
> $$
> f(x_1) = 0.8038754325\\
> f'(x_1) = 3.694177647
> $$
> 
> **Iteration 2:**
> 
> $$
> x_2 = 1.282352947 - \frac{0.8038754325}{3.694177647} = 1.06474335
> $$
> 
> $$
> f(x_2) = 0.1420618031\\
> f'(x_2) = 2.388460097
> $$
> 
> **Iteration 3:**
> 
> $$
> x_3 = 1.06474335 - \frac{0.1420618031}{2.388460097} = 1.00526494
> $$
> 
> $$
> f(x_3) = 0.0106130388\\
> f'(x_3) = 2.03158964
> $$
> 
> **Iteration 4:**
> 
> $$
> x_4 = 1.00526494 - \frac{0.0106130388}{2.03158964} = 1.000040933
> $$
> 
> $$
> f(x_4) = 0.000081871\\
> $$

## Pseudocode

> Note: Keeping track of the delta is optional. We store it in case it gets too small to do calculations with.
> - *(Delta is just the $f(x_i) / f'(x_i)$ portion.)*

In the real world, getting the derivative is no small feat. Next, we'll cover the secant method, which'll allow us to approximate the derivative.

# Secant Method

> Approximate Newton

$$
\boxed{
	x_i = x_{i-1} - \frac{
		(x_{i-1} - x_{i-2}) f(x_{i-1})
	}{
		f(x_{i-1}) - f(x_{i-2})
	}
}
$$

> **Example**: Deriving the Secant Method
> 
> This concept, the definition of the derivative, will be used to approximate.
> 
> Derivative of $f$
> 
> $$
> \frac{
> 	f'(x_0)
> }{
> 	dx
> } = \lim TODO
> $$
> 
> We'll adapt Newton's method so that, given two points, x_a and x_b, we do
> 
> $$
> $$
> 
> to approximate ???.
> 
> Because the slope is an approximation, this method will be faster than Bisection, but slower than Newton.
> 
> Thus and so blah blah blah
> 
> $$
> \text{Update} x_i = x_{i-1} - \frac{f(x_{i-1}}{
> \frac{
> big thing
> }}
> $$
> 
> Final formula (simplified):
> 
> $$
> \boxed{
> 	x_i = x_{i-1} - \frac{
> 		(x_{i-1} - x_{i-2}) f(x_{i-1})
> 	}{
> 		f(x_{i-1}) - f(x_{i-2})
> 	}
> }
> $$

---

> **Example**: Applying Secant Method
> 
> $$
> f(x) = 3x^2 - 4x + 1 \\
> x_0 = 2 \quad
> x_1 = 1.8
> $$
> 
> f(x_0) = 5 f(x_1) = 3.52
> 
> it 1
> 
> x_2 = \frac{1.8 - (1.8 - 2) 3.52}{
> 	3.52 - 5
> } = \frac{49}{37}
> 
> f(49/37) = 0.9642074507
> 
> it 2
> 
> x_3 = \frac{(49/37 - 1.8) f(49/37)}{
> 	f(49/37) - 3.52
> } = 1.144869205
> 
> et cetera blet behakjsdhjakhdk
> 
> it 3
> 
> x_4 ends up being 1.041364782
> 
> f(x_4) = 0.0878627007
> 
> it 4
> 
> x_5 = 1.00702600472
> 
> f(x_5) = 0.0142001024341
> 
> Let's stop here. We are faster than bisection, which at this point had one less zero, and slower than Newton, which had seven zeroes by this point.

## Pseudocode

```clrs
ALGORITHM TODO
```

Superfluous Code: Makes sure algorithm always points to zero, but then, the professor realized that mathematically speaking, it should be removed.
- Remove the second superfluous block, but the first one (first the first iteration) is fine to keep just to ensure order for that one.

### Analysis

<!--
When implementing, we can do hybrid???

Start with one thing and switch partway through???

Professor recommends doing Bisection because it goes fast at the start (because it goes by half every iteration, but takes forever to converge), and once you do like 5 iterations, you can switch to Newton or Secant.

Don't go past 5.

This has the bonus of possibly shooting-past points Newton or Secant would get stuck
-->
